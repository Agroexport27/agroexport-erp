import EnConstruccion from "@/components/EnConstruccion";

export default function EmpaquePage() {
  return (
    <EnConstruccion
      modulo="Empaque"
      descripcion="Aquí van a vivir el inventario de materiales de empaque y las recetas por tipo de empaque (caja + separador + etiqueta + tarima)."
    />
  );
}
